#include<bits/stdc++.h>
using namespace std;

int fun(string n) {
    if(stoi(n) < 10) return 1;

    int steps = 0;
    for(int i=0; i<n.size(); i++) {

    }
}

int main() {
    int n;
    cin >> n;

    int ans = fun(to_string(n));
}