#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, a,b;
    cin >> n >> a >> b;

    vector<int> arr(n);
    for(int i=0; i<n; i++) cin >> arr[i];

    int max_sum = 0;
    int left = 0;

    for(int right=0; right<n; right++){
        if(right-left+1)
    }
}