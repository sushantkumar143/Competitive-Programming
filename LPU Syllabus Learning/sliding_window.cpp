#include<bits/stdc++.h>
using namespace std;

int fun(vector<int>& arr, int k) {
    int left = 0;
    unordered_map<int, int> mp;
    int n = arr.size();
    int cnt = 0;

    for(int i=0; i<k; i++) {
        if()
    }    
}

int main() {
    int n;
    cin >> n;

    vector<int> arr(n);
    for(int i=0; i<n; i++) cin >> arr[i];

    int k;
    cin >> k;

    cout << fun(arr, k);
}